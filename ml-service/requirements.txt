# ============================================================
# ClimaGPT — Stage 1 Requirements
# ============================================================

# Core Django
Django>=4.2,<5.0

# Django REST Framework
djangorestframework>=3.14

# PostgreSQL adapter
psycopg2-binary>=2.9

# Environment variable management
django-environ>=0.11

# HTTP requests (Mapbox API)
requests>=2.31

# CORS headers (for future frontend integration)
django-cors-headers>=4.3

# ============================================================
# Development / Testing
# ============================================================
# (no extra test libs required — uses Django's built-in TestCase + DRF APITestCase)
